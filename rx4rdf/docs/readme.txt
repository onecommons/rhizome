This folder contains a static export of rx4rdf site. You can regenerate it with this command:

rx4rdf\site> run-raccoon -x -a site-config.py -export ../docs -static

(External images are not exported -- you'll have to check them out of CVS.)